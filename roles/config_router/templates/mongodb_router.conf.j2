# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

# where to write logging data.
# systemLog:
#   destination: syslog
#   logAppend: true
#   path: /var/log/mongodb/mongod.log

# security
# security:
#   keyFile: /data/mongossl.key

# network interfaces
net:
  port: 27017
  bindIp: 0.0.0.0

# ConfigDB
sharding:
  configDB: {{ hostvars[groups['config_servers'][0]]['replica_set_name'] }}/{% for item_host in groups['configrs1'] %}{% for shardings_config in hostvars[item_host]['shardings_config'] %}{{ item_host+":"+shardings_config['port'] }}{% if loop.index < loop.length %}{{ "," }}{% endif %}{% endfor %}{% endfor %}


